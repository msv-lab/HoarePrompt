#This is a function and its postcondition is : The function accepts a parameter `n`, returns `n` if `n` is even. If `n` is odd, it returns `n + 1`.
def func(n):
    #This is a if-else statement and its total postcondition is : `n` is an integer. If `n` is even, the function returns the integer value of `n`. Otherwise, the function returns `n + 1`.
    #This is a if statement and its postcondition is : `n` is an integer, and the function returns the integer value of `n`
    if (n % 2 == 0) :
        #This is a return statement in if part and its postcondition is : `n` is an integer, and the function returns the integer value of `n`
        return n
    #This is a else statement and its postcondition is : `n` is an integer, and the function returns `n + 1`
    else :
        #This is a return statement in else part and its postcondition is : `n` is an integer, and the function returns `n + 1`
        return n + 1
