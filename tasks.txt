def func_1():
    x = 10

y = 20

print('Sum:', x + y)
    try:
        z = max(x, y)
    except (Exception):
        print(e)
    #State of the program after the try-except block has been executed: `x` is 10, `y` is 20, `z` is 20. No exception is raised, so the value of `e` is not printed.
    print('Max:', z)

pass
#Overall this is what the function does: The function accepts no parameters and returns a value.

