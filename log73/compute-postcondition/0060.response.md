Output State: **`tasks` is a list of strings containing at least 1 task, `running` is true, `completed` is the total count of 'complete' tasks in `tasks', `pending` is the total count of 'pending' tasks in `tasks', `task` is the first task in the list, and 'Unknown task' is printed.**