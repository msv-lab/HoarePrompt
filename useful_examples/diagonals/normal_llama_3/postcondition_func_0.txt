The function accepts a square matrix of integers, returns the sum of the elements on the main diagonal and the anti-diagonal if the matrix size is greater than 0, otherwise returns 0. However, if the input matrix is not square, the function will raise an IndexError.