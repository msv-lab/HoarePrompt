Postcondition: ***`x` and `y` are integers. If `x` is equal to `y`, then `z` is equal to `2 * x`. If `x` is not equal to `y` and `x` is less than or equal to `y`, then `z` is a positive integer equal to `y - x`.***