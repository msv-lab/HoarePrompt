Postcondition: ***`x` is an integer less than or equal to 0. If `x` is less than -10, then `x` is decreased by 10. If `x` is greater than or equal to -10 but less than or equal to 0, then `x` remains unchanged.***